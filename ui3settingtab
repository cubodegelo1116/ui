local SettingsTab = {}

function SettingsTab:Create(Window)
	local Settings = Window.CreateTab("Settings")

	Settings.AddDropdown("Theme", {
		"Default",
		"Ocean",
		"Purple",
		"Red",
		"Neon"
	}, function(value)
		Window.SetTheme(value)
	end)

	return Settings
end

return SettingsTab
